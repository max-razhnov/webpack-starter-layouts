{
  "name": "webpack-layout-pack",
  "version": "1.0.0",
  "description": "This is the starter pack for simple layouts with HTML, CSS/Less/Sass, where entry point is the javascript file.",
  "main": "./src/app.js",
  "scripts": {
    "start": "webpack-dev-server --mode development --hot --colors --progress",
    "build": "webpack --mode production --compress --colors --progress",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Max Razhnov",
  "license": "ISC",
  "devDependencies": {
    "css-loader": "^3.2.0",
    "express": "^4.17.3",
    "extract-text-webpack-plugin": "^4.0.0-beta.0",
    "html-webpack-plugin": "^3.2.0",
    "style-loader": "^1.0.0",
    "url-loader": "^2.1.0",
    "webpack": "^4.39.3",
    "webpack-cli": "^3.3.7",
    "webpack-dev-server": "^3.8.0"
  }
}
